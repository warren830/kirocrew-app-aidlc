# 租约冲突与失效旧页面 · 2026-09-11

用户反馈指向已结束的 `QA · grouped answers / grouped-proof-v2` 验证任务。
当前 `localhost:5476` 返回该仓库及操作不存在，租约列表为空；旧窗口仍保留旧题单、
旧文案和确认框。该测试目录的文件及历史验证材料已保留，Studio 登记此前已注销。

## 修复

1. 纯 `lease_conflict` 返回 `repo_busy` 与占用者信息，不再称为 AI-DLC 文件矛盾。
   执行租约和管理租约均覆盖；占用者租约保持原样，没有强制释放或插入另一回合。
   确认面板内可直接查看占用仓库的操作。真正的文件矛盾仍按 `state_inconsistent` 处理。
2. 当操作或仓库明确返回不存在时，即使页面保有旧数据，也显示失效页并撤去确认框。
   返回当前待办会清除旧 action、repo、intent 等路由过滤条件。
3. 待确认汇总或计划的题组显示对应标题及后果说明，不再声称有 0 个问题待答；
   已没有待答问题时，不再提供“Draft all answers”。
4. 四个验证会话标为“验证已结束”，保留消息历史，避免当作仍需执行的工作。

## 验证

- 2717 项后端、387 项前端测试全部通过，2264 个中英文词条和构建检查通过。
- 原租约分类在两种真实 Storage 租约夹具上失败，修复后通过；占用者代次和投递状态未变。
- 前端回归先打开确认框，再让操作返回 404，确认旧确认框消失且没有调用发送接口。
- 实际浏览器打开已移除卡片链接，显示“此操作已不可用”，没有发送按钮；
  点击“查看当前待办”后回到当前队列。
- 实际 KiroCrew 窗口已重新载入当前队列，显示 4 项当前记录和 0 个执行租约。
- 本轮没有发送业务答案或 `Looks correct`，也没有新增执行工作流的验证仓库。

本次问题同时涉及旧页面与错误分类，不把“等待另一操作释放租约”解释为文件内容损坏。
