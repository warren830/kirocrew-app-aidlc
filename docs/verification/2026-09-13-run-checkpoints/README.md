# Run 过期清理与启动状态核对

审批或问答出现后，待办应呈现该检查点；旧 Run 不能继续请求另一个回合。
阶段已经变化的 Run 也不能通过刷新证据重新获得发送资格。

## 行为与验证

| 行为 | 验证 |
|---|---|
| 审批、普通问题、总结确认、计划审批出现后关闭旧 Run | 真实 reader、数据库、HTTP 路由和扫描集成测试；审批或问题卡继续保留 |
| 阶段变化、任务完成后关闭旧 Run | 扫描与提交两条路径均覆盖 |
| 执行中、审批或问答期间不创建新 Run | 后端拒绝且不新增记录；前端按钮禁用 |
| 投递前新出现的问题仍能阻止 Run | 在取得租约后模拟问题出现；无投递记录，临时租约释放 |
| 并发投递与过期清理相遇 | 代次比较阻止清理覆盖正在投递的记录 |
| 已投递操作保持完整 | 清理不取消已投递记录、不释放其租约、不重发 |
| 已打开的确认框随记录失效而关闭 | DetailShell 真实模板组合测试通过 |
| 修订仍可继续执行 | `[R]` 不被误判为人工审批 |

主修复的全量检查通过：2756 项后端、398 项前端，
以及类型检查、双语目录、构建、网关解释器导入和载荷完整性检查。
启动判断的补充修复通过 137 项相关回归。

## 实际运行记录

- 旧 Run `a_417b68b189525613` 从未发送，已通过安全取消接口撤下，历史保留。
- 当前 Code Generation 会话持续运行；新版 Intents 页面显示 Running，并禁用 Run 按钮。
- Action Center 不再显示旧 Run。
- 运行文件与工作区校验一致；没有额外发送任何会话消息或审批答案。

## 重载期间发现的问题与恢复

首次更新期间，旧的启动逻辑在宿主尚未连接时，把计划审批回执视为回合结束，
提前结算了 `a_015dc83090603d81`。会话本身没有停止，原始消息和工作流文件也没有改变。

已去除这一推断：宿主不可读时不判定回合结束。新增冷启动回归使用已记录的计划审批、
仍在运行的会话及未连接的 HostBridge，验证租约一直保留至宿主确认会话结束。

这一次误结算的 Studio 记录已从更新前的一致性备份恢复：

- 状态恢复为 Processing，保留原 `dl_313f9c5141a90c4c` 和人工回合基线。
- 动作代次继续递增，租约代次从 10 提升为 11；没有复用旧持有者代次。
- 原误结算历史未删除，另记 `startup_settlement_reverted` Activity 及本机修复日志。
- 最终 boot_id 为 `45e2955f967b0909`，健康状态正常，界面显示 1 个运行回合、1 个租约。
- 会话消息列表与更新前一致，44 个工作流文件 SHA-256 全部一致。

备份、原始验证日志和修复前后记录位于本机：
`/Users/ychchen/warren_ws/aidlc-studio-backups/run-checkpoint-20260913-115043`。
CUA 截图已在本次会话中展示；没有操作正在执行回合的命令授权。
